<%= form_with(model: [@recycler, @material], local: true) do |form| %>
  <div class="mb-3">
    <%= form.label :base_material_id, "Selecciona un material base" %>
    <%= form.collection_select :base_material_id, @base_materials, :id, :name, { prompt: "Selecciona un material base" }, class: "form-control" %>
  </div>
  
  <div class="mb-3">
    <%= form.label :name, "Nombre del material" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>
  
  <div class="mb-3">
    <%= form.label :description, "Descripción del material" %>
    <%= form.text_area :description, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.submit "Guardar", class: "btn btn-primary" %>
  </div>
<% end %>
