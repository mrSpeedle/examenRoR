<h1>Nuevo Material Reciclado</h1>

<%= form_with(model: [@recycler, @material], local: true) do |form| %>
  <div class="mb-3">
    <%= form.label :name, "Material" %>
    <%= form.text_field :name, class: "form-control", required: true %>
  </div>

  <div class="mb-3">
    <%= form.label :description, "Descripción" %>
    <%= form.text_area :description, class: "form-control", required: true %>
  </div>

  <%= form.submit "Registrar Material", class: "btn btn-success" %>
<% end %>

<%= link_to "Volver", materials_recycler_path(@recycler), class: "btn btn-secondary mt-3" %>
